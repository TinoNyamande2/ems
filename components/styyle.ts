import { constants } from "crypto"

export const siteTitle = "EMS"
export const pageContainer = {
        mt: { xs: 8, sm: 2 },
        alignSelf: 'center',
        height: "auto",
        width: 'auto',
        backgroundSize: 'cover',
        borderRadius: '10px',
        outline: '1px solid',
        padding: { sm: 4, xs: 2 },
        boxShadow: `0 0 12px 8px lightgray`
}
export const tableHead= {
     fontSize: {sm:"1em",md:"1.3em",xs:"0.8em"}, 
     fontWeight: "bold",
     padding: { xs: 0.5, sm: 2 } 
}

export const tableCell= {
    fontSize: {sm:"1em",md:"1.3em",xs:"0.8em"}, 
    padding: { xs: 0.5, sm: 2 } 
}

export const tableContainer = {
     padding: { xs: 1, sm: 2 }, 
    margin: { xs: 1, sm: 2 } 
}

